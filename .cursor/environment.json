{
  "name": "NMTS Sleeping Stock Web",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "backend",
      "command": "cd backend && ARCHIVE_PRUNE_ENABLED=false ./venv/bin/python -m uvicorn server:socket_app --host 0.0.0.0 --port 8000",
      "description": "FastAPI + Socket.IO API on port 8000 (ASGI entrypoint server:socket_app, run from the project venv)."
    },
    {
      "name": "frontend",
      "command": "cd frontend && npm start",
      "description": "React (CRA/craco) dev server on port 3000. Uses frontend/.env.local to reach the local backend."
    }
  ],
  "ports": [
    { "name": "backend", "port": 8000 },
    { "name": "frontend", "port": 3000 }
  ]
}
